[@math]
node add as "Add" {
    "Add two numbers"

    in val1: number as "V1" {
        decoration: 2 digits,
        min: 0,
        max: 1,
    }

    out result: number as "Result"

    `
    --some code that the node needs for it to work
    `

    on val1, !tick `
        output_result(123)
    `
}

